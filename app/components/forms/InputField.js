import React from 'react'
import {View} from 'react-native'

export default class InputField extends React.PureComponent {
  render() {
    {/*const {type, label, input, meta, viewer} = this.props;*/}
    {/*const style = !!meta.touched ? (!!meta.error ? 'danger' : 'success') : '';*/}
    {/*const feedback = !!meta.touched ? (!!meta.error ? <FormFeedback>{meta.error}</FormFeedback> : '') : '';*/}

    {/*if (!!viewer) {*/}
    //   return (
    //     <FormGroup>
    //       <Label>{label}</Label>
    //       <Input static>{input.value}</Input>
    //     </FormGroup>
    //   );
    // }

    return (
      <View></View>
      // <FormGroup color={style}>
      //   <Label>{label}</Label>
      //   <Input type={type} {...input} placeholder={label} state={style}/>
      //   {feedback}
      // </FormGroup>
    );
  }
}
