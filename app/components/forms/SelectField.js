import React from 'react'
import {View} from 'react-native'
// import {FormGroup, Input, Label, FormFeedback} from 'reactstrap'

export default class SelectField extends React.PureComponent {
  render() {
    // const {label, input, meta, children} = this.props;
    // const style = !!meta.touched ? (!!meta.error ? 'danger' : 'success') : '';
    // const feedback = !!meta.touched ? (!!meta.error ? <FormFeedback>{meta.error}</FormFeedback> : '') : '';
    return (
      <View></View>
      // <FormGroup color={style}>
      //   <Label>{label}</Label>
      //   <Input type="select" {...input} placeholder={label} state={style}>
      //     {children}
      //   </Input>
      //   {feedback}
      // </FormGroup>
    );
  }
}
