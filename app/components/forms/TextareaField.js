import React from 'react'
import {View} from 'react-native'
// import {FormGroup, Input, Label, FormFeedback} from 'reactstrap'

export default class TextareaField extends React.PureComponent {
  render() {
    // const {label, input, meta} = this.props;
    // const style = !!meta.touched ? (!!meta.error ? 'danger' : 'success') : '';
    // const feedback = !!meta.touched ? (!!meta.error ? <FormFeedback>{meta.error}</FormFeedback> : '') : '';
    return (
      <View></View>
      // <FormGroup color={style}>
      //   <Label>{label}</Label>
      //   <Input type="textarea" {...input} placeholder={label} rows="5" state={style}/>
      //   {feedback}
      // </FormGroup>
    );
  }
}
